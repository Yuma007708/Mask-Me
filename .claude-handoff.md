# Mask-Me 作業引き継ぎ（2026-07-01）

このファイルは Claude Code 環境間で作業を引き継ぐためのスナップショット。
**git tracked にしてリポジトリに含めてあるので、clone した cloud / 別マシンからも見える。**

---

## 🤖 クラウド/別マシンで Claude を起動するときに貼る指示（コピペ用）

```text
あなたは Mask-Me iOS アプリの開発を引き継いでください。
GitHub: https://github.com/Yuma007708/Mask-Me
作業ブランチ: fix/video-face-detection

最初に以下を実行：

  git clone --branch fix/video-face-detection https://github.com/Yuma007708/Mask-Me.git
  cd Mask-Me
  cat .claude-handoff.md

`.claude-handoff.md` の「アクションリスト」に従って続行してください。

制約：
- iOS Simulator / xcodebuild / xcodegen / pod install は macOS 専用、cloud では使えません
- ローカル側の /tmp/dvalid.log は cloud からは見えません
- GitHub Actions の CI 操作（gh CLI）と git ベースのコード変更に絞ってください

区切りで止まるときは必ず `.claude-handoff.md` を最新状況に更新して push してください。
```

---

## 状況サマリ（2026-07-02 時点・最新）: 精度向上プロジェクト進行中

**目標: モザイク加工の精度（顔検出率・追従率）を限りなく100%に近づける。**
計画全文はローカル `/Users/tatsuki/.claude/plans/vivid-swimming-wave.md`（Phase 0〜4）。

### 実装済み（fix/video-face-detection、コミット順）

1. **Phase 0 計測インフラ（`6429717`）**: `MosaicCore.DetectionBridge`（lookupFaces/lookupCache
   の共通化）、DValid に bridgedRate（アプリ補間込み追従率）/bridgedRate10/[DVALFRAME]
   タイムライン/検出ソース別カウンタ（srcMp/srcEnh/srcBbox/srcRoi/srcLow）を追加。
   Aggregate に新列。**挙動不変**。baseline 取得 CI = Run #28560141992（実行中）
2. **Phase 1 テンポラル ROI 再検出（`e180e3f`）**: 全画面パイプライン全滅時に前フレーム顔
   bbox の 2.0 倍 ROI を IMG モードで再走査（256px 未満は upscale）。継続性ガード
   IoU>0.1・面積比 0.3-3.0、maxMisses=8、timestamp 巻き戻し/1秒超ギャップでリセット
3. **Phase 2 検出フォールバック束（`cbdcda0`）**: 低 conf(0.05) 全画面最終フォールバック
   （ROI track の再シードが主効果）+ YuNet scoreThreshold 0.6→0.5
4. **Phase 3 追従層（`8c54dcb`）**: DetectionBridge の lerp 補間モード（アプリで有効化）+
   `LandmarkSmoother`（描画直前 EMA α=0.5・ジャンプ>0.05 スナップ、プレビュー/エクスポートに配線）

### ローカルプローブ結果（off backend、劇的改善）

| プローブ | 改善前(CI全体) | Phase 2 後 | 主要因 |
|---|---|---|---|
| s1_off_A | 12.6% | **71.4%**（lowRate 0.0%） | roi 332 + low 7 の相乗 |
| s3_off_A | 13.2% | **23.9%**（lowRate 3.5%） | roi 215 |
| s4_off_A | 16.1%(A半) | 19.3%（lowRate 不変） | low 7 |
| s5_off_B | 48.7%(B半) | 53.5%（lowRate 6.4% ≤ガード9%） | roi 45 + low 28 |

低 conf フォールバック単体の直接ヒットは少ないが、「track 消滅後の再シード」として
ROI 追跡を復活させる相乗効果が大きい（s1 で roi 97→332）。

### 次の一手

1. baseline CI（Run #28560141992）完走確認 → rate/lowRate が旧値 ±1pp（Phase 0 不変確認）
2. HEAD（Phase 1+2+3 込み）でフル CI 起動 → baseline と比較して公式計測
   （off=ROI+lowConf の効果、yunet−off=YuNet 0.5 の効果、bridgedRate=追従率）
3. CI artifact の [DVALFRAME] で bridgeWindow {5,8,10,15}/15・EMA α をオフラインスイープ
   → bridgeWindow 拡大の採否（bridgedRate10 は s4 で +6.3pp と有望）
4. s5 lowRate が全体で 9% を超えたら低 conf/ROI のガード強化を検討
5. （任意）eyeRatio 0.38 実験は s1 が大幅回復したため優先度低下

## 状況サマリ（2026-07-01 時点）

### scanner 定期再生成の flaky 対策は不採用（悪化を確認）

コミット `f7834e5`（200 フレームごとに MediaPipe FaceLandmarker を作り直す）を
CI Run #28505454271 で検証した結果、**効果がないどころか悪化**した：

- 修正前（Run #28496967052）: `s1/off`・`s3/yunet`・`s5/off` の3ジョブのみ失敗
- 修正後（Run #28505454271）: 上記に加え **`s4/off`（38秒、これまで一度も失敗していない
  短尺動画）と `s5/yunet` も新たにクラッシュ**。ログを見ると `s4/off` はフレームループ
  終盤で無言でテストプロセスが再起動（`Restarting after unexpected exit, crash, or test
  timeout`）→ 0 tests executed で `TEST FAILED`（exit code 65）。
- 旧コード（scanner 使い回し）では `s4/off` は問題なく通っていた実績があるため、
  **scanner を実行中に作り直すこと自体が Simulator 上で新しい不安定要因になっている**
  と判断し、この対策は revert（コミット `dbe81d2`）。

### 新たに判明した別の失敗モード：ジョブタイムアウト

同じ Run で `s1/yunet`・`s1/faceDetector`・`s3/off` の3ジョブが `cancelled` に
なっていたが、これは **クラッシュではなく `timeout-minutes: 60` にほぼ正確に
到達したことによるタイムアウト**（60:32〜60:41 で揃って切れている）。
`s1/faceDetector` は「安定している」とされてきた backend だったため、これは
クラッシュ系の flaky とは別に、**長尺動画の処理自体が1時間を超えかねないほど遅い
（またはランナーが混雑している）** という新しい問題。

### 対策：動画を前半/後半に分割して別プロセスで実行

コミット `dbe81d2` で対応：

- scanner 定期再生成を revert し、旧来通り 1 テストにつき 1 scanner インスタンスに戻した
- 各動画・各 backend のテストを **前半 (A) ・後半 (B) の2つの XCTest メソッドに分割**
  （`test_S1_off_A` / `test_S1_off_B` など、計 30 メソッド）。CI matrix にも `half: [A, B]`
  軸を追加し、`.github/workflows/dvalid.yml` の Aggregate ステップで A+B を合算して元の
  「動画全体」相当の rate に戻して表示する
- 狙い: 1 プロセスあたりの累積 detect 呼び出し数・実行時間を半分にすることで、クラッシュ
  条件（ネイティブリソース蓄積と推測）にもジョブタイムアウトにも到達しにくくする
- ローカルで `xcodegen generate` → `pod install` → `xcodebuild build-for-testing` /
  `-only-testing:MaskMeTests/DValidVideoTests/test_S1_off_A`（動画無し・XCTSkip）まで確認済み。
  実動画での検証は CI 待ち。
### 前後半分割の検証結果（Run #28523026472）と追加対策：CI リトライ

Run #28523026472（`videos=s1,s3,s4,s5` `backends=off,faceDetector,yunet` `halves=A,B` の
24ジョブ、s2は安定実績があるため除外）の結果:

- **timeout-minutes によるキャンセルは完全に解消**（前回3件→0件）。前後半分割で
  1ジョブあたりの実行時間が短くなったことが効いている
- **クラッシュ失敗率は改善したが解消はしなかった**: 24ジョブ中5件失敗
  （`s1/yunet/B`・`s1/faceDetector/A`・`s3/yunet/A`・`s4/faceDetector/B`・`s5/faceDetector/B`）。
  分割前は15ジョブ中8件が失敗/cancelledだったので、率としては約53%→21%に改善
- **重要な発見**: 失敗した5件のログを確認したところ、全て同じ
  `Restarting after unexpected exit, crash, or test timeout` → `Executed 0 tests` →
  `TEST FAILED` (exit 65) のパターン。**これまで一度もクラッシュしたことがなかった
  `.faceDetector` backend でも今回3件クラッシュしている**（s1/A, s4/B, s5/B）。
  つまりこの crash は「長尺動画 + off/yunet」に固有ではなく、**backend や動画長を問わず
  確率的に起きるプロセスクラッシュ**である可能性が高い。クラッシュレポート（.ips や
  xcresult 内の diagnostic）は workflow 側で一切収集していないため、スタックトレース
  ベースの原因究明はまだできていない

この状況（3系統すべてのbackendで、動画長に関係なく確率的にクラッシュ）から、
「MediaPipe VIDEO モードの内部状態蓄積」という当初の仮説だけでは説明がつかなくなり、
CI ランナー（macOS Simulator）側のインフラ起因の flaky である可能性を重視することにした。

**対策（コミット `cc064cb`）**: `.github/workflows/dvalid.yml` の `Run XCTest` ステップで
`xcodebuild test` が失敗したら **最大3回まで自動リトライ**するようにした
（`until ... do ... done` ループ、`PIPESTATUS[0]` で xcodebuild 自体の exit code を見る）。
前後半分割で失敗率を下げた上に、残った確率的クラッシュはリトライで吸収する狙い。

### リトライ検証の結果（Run #28554824392）— ほぼ有効、attempt単位のクラッシュ率は高い

Run #28554824392（`videos=s1,s3,s4,s5` `backends=faceDetector,yunet` `halves=A,B` の
16実ジョブ、`off`は成功実績ありのため除外）の結果:

- **16ジョブ中15成功・1失敗**（前回失敗した5組み合わせはすべて成功）
- **リトライで救済されたジョブが6件**: s1/yunet/A・s1/yunet/B・s3/faceDet/B・
  s4/yunet/A・s4/faceDet/A（各1回リトライ）、s3/yunet/B（2回リトライ）
- **attempt単位でみたクラッシュ率は約40%**（全25 attempt中10クラッシュ）。
  つまり基盤の不安定さ自体は全く解消しておらず、リトライで吸収できているだけ
- **唯一の失敗 s3/faceDetector/A は同一ランナー上で3回連続クラッシュ**
  （attempt1: 27分後、attempt2: 4分後、attempt3: 14分後）。同一ランナーで
  リトライしても救えないケースがあることを示す（ランナーVM依存の可能性）。
  → `gh run rerun --failed` で別ランナーによる再実行を実施
- **検出精度は baseline 比 ±3.4pp 以内で前回 Run #28450221341 と一致**。
  前後半分割による計測の歪みはない。s1 の -3.4pp は既知（eyeRatio 0.40 の影響検証は保留中）

**結論**: 「前後半分割 + 3回リトライ」で運用上はほぼ緑になる。ただし attempt 単位の
クラッシュ率40%は異常に高く、根本原因（Simulator上のMediaPipeプロセスの確率的クラッシュ）
は未解明のまま。

- **次の一手**:
  1. s3/faceDetector/A の rerun 結果確認（別ランナーなら通る見込み）
  2. 根本原因を追うなら: workflow に xcresult / `~/Library/Logs/DiagnosticReports` の
     クラッシュログ (.ips) を artifact 化するステップを追加し、クラッシュシグナル
     （SIGSEGV/SIGABRT/OOM-kill）を特定する
  3. 運用を優先するなら: 現状の分割+リトライ構成で凍結し、精度チューニング
     （s1 の -3.4pp、s5 の lowRate 8.8%）に戻る

### 直近の重要発見（2026-06-30・履歴）

CI Run #28329255676 の結果で **動画ファイル割当のズレ** が発覚：

- 旧 workflow が Drive 動画をサイズ判定でマップ → ローカル baseline 計測時の s2 が CI では s3 にズレていた
- これで S3 の +54pp 改善 / S2 の -44pp 悪化が見せかけだった
- ファイル名固定マップに修正（commit `16f1b57`）し、新 CI Run #28450221341 で再計測中

### 最新コミット履歴（fix/video-face-detection）

```
16f1b57 fix(ci): 動画ファイル名固定マップ化 + Aggregate python heredoc 化
c4f43b7 test: remove XCTAssert thresholds from DValidVideoTests
571a423 test: wrap frame loop in autoreleasepool to prevent OOM crash
f0ada6a fix: inject SAMPLE_VIDEO_DIR into xcscheme TestAction instead of -testenv
d5059a6 fix: pass SAMPLE_VIDEO_DIR to xcodebuild test process via -testenv
5ebd2ac fix: [DVALRESULT] 出力を print→fputs(stderr) に変更
0629135 docs: cloud Claude に「区切るとき handoff 更新 + push」を指示
aa243dc docs+ci: ローカル DValid 全結果の反映と CI flatten 修正
0a44a22 ci: gdown 出力と DL 後の ls を見せて Map ステップ失敗原因を可視化
3490728 docs: .claude-handoff.md を git tracked に格上げ + クラウド引き継ぎプロンプト追加
```

---

## CI 履歴

| Run# | ID | 内容 | 結果 |
|---|---|---|---|
| 28206652811 | Xcode 自動検出 + OS=latest | 部分失敗（その後の cloud 修正で解消） |
| 28329255676 | s4/off rerun で 15/15 緑、Aggregate のみ失敗 | s4/off は flaky だった |
| **28450221341** | **動画マップ修正 + Aggregate heredoc 化** | **11/15 完走、Aggregate 成功。s1 と s3 の off/yunet が長尺×非 faceDetector 組み合わせで構造的 flaky** |

---

## Run #28450221341 最終結果（マッピング修正後）

| 動画 | backend | hit/total | rate | lowRate | baseline | Δ |
|---|---|---|---|---|---|---|
| s1 | faceDetector | 176/1383 | 12.7% | 0.0% | 16% | **-3.3pp** |
| s1 | off / yunet | (構造的 flaky) | — | — | — | — |
| s2 | 全 backend | 253/439 | 57.6% | 0.2% | 58% | -0.4pp |
| s3 | faceDetector | 314/2384 | 13.2% | 1.3% | 13% | +0.2pp |
| s3 | off / yunet | (構造的 flaky) | — | — | — | — |
| s4 | 全 backend | 59/569 | 10.4% | 1.8% | 10% | +0.4pp |
| s5 | off / faceDetector | 806/1557 | 51.8% | 8.7% | 49% | +2.8pp |
| s5 | yunet | 809/1557 | 52.0% | 8.7% | 49% | +3.0pp |

**結論**:
- S2 が -44pp に見えていた原因は動画ファイル割当のズレ（修正済）
- 全動画で baseline ±3pp 以内、コード変更（eyeRatio 0.40 + bbox guard + YuNet letterbox）に回帰なし
- 3 backend (off/faceDetector/yunet) は S1〜S4 で hit 完全一致、S5 でだけ yunet が +3 件追加（letterbox の効果が S5 特定シーンで現れる）

## flaky パターン

s1/off, s1/yunet, s3/off, s3/yunet は連続 rerun でも再失敗。`.faceDetector` は通る。
- 動画長 92秒以上 + 補助検出器が MP FaceLandmarker 共有以外（`.off`=なし, `.yunet`=YuNet）
- テストプロセスがループ末尾近辺（≒ 全フレーム処理完了直前）でクラッシュ → 0 tests executed
- 短尺 (s2=29s, s4=38s) や `.faceDetector` バックエンドでは発生せず
- s5 (104s) は時々発生

**今後の対策候補**:
1. テスト内の scanner を 200 フレームごとに再作成（MediaPipe キャッシュ抑制）
2. interval 1/15 → 1/30 で処理量半減（精度比較として弱くなる）
3. 1 テスト = 1 動画半分（前後分割で 2 テスト化）

---

## 動画ファイル ↔ Drive オブジェクト固定マップ

| s番号 | Drive ファイル名 prefix | サイズ | 長さ | 由来 |
|---|---|---|---|---|
| s1 | copy_4D828ACB | 65MB | 92s | ローカル baseline 計測時の s1 |
| s2 | copy_0C57704F | 13MB | 29s | ローカル baseline 計測時の s2 |
| s3 | copy_EB41EC4E | 48MB | 159s | Drive 新規動画（baseline は CI 初回 13.2%） |
| s4 | copy_5B417FE2 | 52MB | 38s | ローカル baseline 計測時の s4 |
| s5 | copy_324B1BD2 | 87MB | 104s | ローカル baseline 計測時の s5 |

`.github/workflows/dvalid.yml` の `Map Drive filenames` ステップで固定マップ済。

---

## 残作業

| 作業 | 場所 | 状況 |
|---|---|---|
| Run #28450221341 完走確認 | cloud で可 | 実行中 |
| 完走後の集計テーブル作成 | cloud で可 | 待ち |
| handoff 最終結果反映 | cloud で可 | 待ち |
| 実機検証（追従率） | macOS + iPhone 専用 | 任意 |

---

## アクションリスト

### A. Run #28450221341 完了待機

```bash
gh run view 28450221341 --repo Yuma007708/Mask-Me --json status,conclusion,jobs \
  --jq '{status, conclusion, failed: [.jobs[] | select(.conclusion=="failure") | .name]}'
```

### B. 完走したらローカル集計（GitHub Actions の Aggregate でも summary に出る）

```bash
SCRATCH=/tmp/ci-results
mkdir -p "$SCRATCH"
gh run download 28450221341 --repo Yuma007708/Mask-Me --dir "$SCRATCH"
python3 <<'PY'
import json, glob, os
root = "/tmp/ci-results"
rows = []
for f in sorted(glob.glob(os.path.join(root, "result-*/result.json"))):
    text = open(f).read()
    s, e = text.find("{"), text.find("}", text.find("{"))
    if s < 0 or e < 0: continue
    rows.append(json.loads(text[s:e+1]))
rows.sort(key=lambda d:(d["video"], d["backend"]))
print(f"{'video':<5} {'backend':<13} {'total':>5} {'hit':>5} {'rate':>6} {'lowCy':>6} {'lowRate':>7} {'baseline':>8}  delta")
for d in rows:
    delta = (d["rate"]-d["baseline"])*100
    print(f'{d["video"]:<5} {d["backend"]:<13} {d["total"]:>5} {d["hit"]:>5} {d["rate"]*100:>5.1f}% {d["lowCy"]:>6} {d["lowRate"]*100:>6.1f}% {d["baseline"]*100:>7.0f}%  {delta:+.1f}pp')
PY
```

### C. 結果が安定したら次の改善候補

- **S5 の lowRate 8.7% が残っている** — 体への誤モザイクが S5 で 136 件。eyeRatio や bbox guard をもう一段締める？
- **S1 -3.3pp** が真に悪化なら eyeRatio 0.40 を 0.38 へ戻す検証
- **CLAHE 相当 enhance 4 段目追加**（小顔・暗所向け）
- **デフォルト backend を `.all`** に変えるか検証

---

## CI ワークフロー リファレンス

| ファイル | 役割 |
|---|---|
| `.github/workflows/dvalid.yml` | 5 動画 × 3 backend = 15 ジョブ並列 |
| `App/MaskMeTests/DValidVideoTests.swift` | (動画, backend) を 1 テストメソッドに展開 |

**Secret**：`SAMPLE_DRIVE_FOLDER_ID = 1fI4wAaaCQT2ZpbY23Sy9PI5H666LaI3E`

**起動**：

```bash
gh workflow run dvalid.yml --ref fix/video-face-detection --repo Yuma007708/Mask-Me
# サブセット
gh workflow run dvalid.yml --ref fix/video-face-detection --repo Yuma007708/Mask-Me -f videos=s3 -f backends=yunet
```

---

## 終わったら更新

このファイルは作業の進捗を記録し続けるための **生きているドキュメント**。
状況が変わったら（CI 完走、追加実装、コミット追加）この `.claude-handoff.md` を更新して push してください。
