# Mask-Me 作業引き継ぎ（2026-06-26）

このファイルは Claude Code 環境間で作業を引き継ぐためのスナップショット。
**git tracked にしてリポジトリに含めてあるので、clone した cloud / 別マシンからも見える。**

---

## 🤖 クラウド/別マシンで Claude を起動するときに貼る指示（コピペ用）

```text
あなたは Mask-Me iOS アプリの開発を引き継いでください。
GitHub: https://github.com/Yuma007708/Mask-Me
作業ブランチ: fix/video-face-detection

最初に以下を実行：

  git clone --branch fix/video-face-detection https://github.com/Yuma007708/Mask-Me.git
  cd Mask-Me
  cat .claude-handoff.md

`.claude-handoff.md` の「クラウド側で続けるアクションリスト」に従って続行してください。

制約：
- iOS Simulator / xcodebuild / xcodegen / pod install は macOS 専用、cloud では使えません
- ローカル側の /tmp/dvalid.log は cloud からは見えません
- GitHub Actions の CI 操作（gh CLI）と git ベースのコード変更に絞ってください

ユーザーがいる方の macOS マシンで進めるべき作業（diagnostic 撜去、シミュレータ検証など）は plan で明示してください。
```

---

## 状況サマリ（2026-06-26 08:25 JST 時点）

### 実装済み（fix/video-face-detection ブランチ、origin に push 済み）

```
8cc0ed7 ci: Xcode 自動検出 + OS=latest 指定で simulator ランタイム未インストールを解消
aa243dc docs+ci: ローカル DValid 全結果の反映と CI flatten 修正
5a4b9a9 ci: gdown の廃止フラグ --remaining-ok を削除
f7b0cba fix: 体への誤モザイク削減 + プレビュー時刻同期
5253d85 feat: YuNet (Core ML) を補助検出器に追加（5 択切替）
```

---

## CI 履歴と変更点

| Run# | ID | 内容 | 結果 |
|---|---|---|---|
| 1 | 28205622347 | --remaining-ok フラグ | 全失敗 |
| 2 | 28205714667 | gdown flatten 前 | 全失敗 |
| 3 | 28206005374 | flatten 修正前 | 全失敗 |
| 4 | 28206320028 | flatten 修正済 | **全失敗** (Xcode 16.0 問題) |
| **5** | **28206652811** | **Xcode 自動検出 + OS=latest** | **起動中** |

### Run #4 の失敗原因（解決済）

- `sudo xcode-select -s /Applications/Xcode_16.0.app` で Xcode 16.0 を強制選択
- iOS 18.0 simulator runtime がインストールされていないランナーで実行された
- `xcodebuild: error: Unable to find a destination... iOS 18.0 is not installed`

### Run #5 の修正内容

1. **Select Xcode**: `ls -d /Applications/Xcode*.app | sort -V | tail -1` で最新を自動選択
2. **Prepare iOS Simulator ステップを削除**: UUID ベースのデスティネーションを廃止
3. **Run XCTest デスティネーション**: `OS=latest,name=iPhone 15` で UUID 不要に

---

## ローカル DValid 検証結果（完了済）

| 動画 | backend | hit | total | rate | lowCy/hit | baseline | 評価 |
|---|---|---|---|---|---|---|---|
| S1 | off / faceDet / yunet | 215 | 1352 | 15% | 0/215 (0%) | 16% | baseline 維持、lowCy ゼロ |
| S2 | off / faceDet / yunet | 244 | 409 | 59% | 0/244 (0%) | 58% | +1pp、lowCy ゼロ |
| S3 | off / faceDet / yunet | 4 | 203 | 2% | 4/4 (100%) | 3%(全誤) | 裸+逆光、根本的に正面顔なし |
| S4 | off / faceDet / yunet | 54 | 569 | 9% | 8/54 (15%) | 10% | baseline 維持、lowCy 少し |
| S5 | off / faceDet | 801 | 1557 | 51% | 137/801 (17%) | 49% | +2pp |
| S5 | **yunet** | **803** | 1557 | 51% | 137/803 (17%) | 49% | letterbox 効果 (+2件) |

**結論**: 全動画で baseline ±1pp 以内、検出率の回帰なし。体への誤モザイク (lowCy) は S1/S2 で 0%。

---

## 残作業

| 作業 | 場所 | 状況 |
|---|---|---|
| CI Run #5 完走待ち | **cloud で可** | 実行中 |
| CI 結果集計（Run #5） | **cloud で可** | Run #5 完走後 |
| CI 結果と baseline 比較表 | **cloud で可** | Run #5 完走後 |
| `MaskMeApp.swift` から DValid 撜去 + コミット | macOS 専用 | 完了済（作業不要） |
| 実機での目視検証 | macOS + iPhone 専用 | 任意 |

---

## クラウド側で続けるアクションリスト

### A. Run #5 の完了を待って確認

```bash
# Run #5 のステータス確認
gh run view 28206652811 --repo Yuma007708/Mask-Me
gh run view 28206652811 --repo Yuma007708/Mask-Me --json jobs --jq '.jobs[] | {name, status, conclusion}'
```

### B. 失敗があればログを確認して修正

```bash
gh run view 28206652811 --repo Yuma007708/Mask-Me --json jobs --jq '.jobs[] | select(.conclusion == "failure") | {name, databaseId, steps: [.steps[] | select(.conclusion == "failure") | .name]}'

gh api repos/Yuma007708/Mask-Me/actions/jobs/<JOB_ID>/logs | grep -iE '(error|fail|xcode|simctl|destination)' | tail -30
```

### C. 完走したら集計

```bash
RUN_ID=28206652811

# 各ジョブの結果テーブル
gh run view $RUN_ID --repo Yuma007708/Mask-Me --json jobs --jq '.jobs[] | select(.name | startswith("s")) | {name, conclusion}'

# Artifact のダウンロード
mkdir -p /tmp/ci-results
gh run download $RUN_ID --repo Yuma007708/Mask-Me --dir /tmp/ci-results

# 結果集計
for f in /tmp/ci-results/result-*/result.json; do
  cat "$f" | grep -oE '\{.*\}'
done | jq -s 'sort_by(.video, .backend)'
```

### D. 次の plan

CI 結果で検出抜けが多ければ：

- **案 A**：`DetectionSettings.minSpan` デフォルトを 0.02→0.01、`faceDetectorBackend` を `.vision` → `.all`
- **案 B**：`MediaPipeFaceLandmarkerAdapter.enhance` に 4 段目 `.clahe` 追加（CIFilter チェーン）

---

## CI ワークフロー リファレンス

| ファイル | 役割 |
|---|---|
| `.github/workflows/dvalid.yml` | matrix で 5 動画 × 3 backend = 15 ジョブ並列 |
| `App/MaskMeTests/DValidVideoTests.swift` | 各 (動画, backend) を 1 テストメソッドに展開、SAMPLE_VIDEO_DIR から `sN.mov` を読む |

**Secret**：`SAMPLE_DRIVE_FOLDER_ID` = `1fI4wAaaCQT2ZpbY23Sy9PI5H666LaI3E`（登録済み）

**起動**：

```bash
gh workflow run dvalid.yml --ref fix/video-face-detection --repo Yuma007708/Mask-Me
# サブセット
gh workflow run dvalid.yml --ref fix/video-face-detection --repo Yuma007708/Mask-Me -f videos=s3 -f backends=yunet
```

---

## サンプル動画の baseline（MP 単独 = `.off`、前回計測値）

- S1 = 16% / S2 = 58% / S3 = 3%（全誤検出） / S4 = 10% / S5 = 49%

---

## Explore 調査結果（前セッション、検出抜け対策）

| 案 | 効果 | 副作用 | 実装 |
|---|---|---|---|
| **CLAHE 相当 enhance（4 段目）** | 小顔・暗所 +15【25% | +10ms / frame | 小（CIFilter チェーン拡張） |
| **`minSpan` デフォルト 0.02→0.01** | 小顔検出 +20% | 単独時の誤検出増（補助検出器で防ぐ前提） | 極小、要検証 |
| **`bridgeWindow` 5→7【10** | 中（補間範囲拡大） | アウト位置固定リスク | 極小、要検証 |
| **デフォルト補助検出器を `.all`** | 検出抜けほぼゼロ | 処理 3 倍、prescan 15fps→5fps | 既実装、default 変更のみ |

---

## 未コミット作業

なし。work-tree は clean。リポジトリ全体が cloud から完全に復元可能な状態。

---

## 終わったら更新

このファイルは作業の進捗を記録し続けるための **生きているドキュメント**。
状況が変わったら（CI 完走、追加実装、コミット追加）この `.claude-handoff.md` を更新して push してください。
